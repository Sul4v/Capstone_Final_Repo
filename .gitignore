# General
.DS_Store
.AppleDouble
.LSOverride
._*
.Trashes

# Editor/IDE
.idea/
.vscode/
*.swp
*.swo

# Node.js / JavaScript (for capstone_final_app)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Next.js
.next/
out/
build/
.vercel/

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Python (for text_evaluations)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
.pytest_cache/
*.spec

# Application Specific
# Ignore generated csvs in evaluations if they are large or sensitive, but typically these might be tracked.
# The user already has them in repo, so I will NOT ignore them by default unless asked, 
# but I *will* ignore any potential API keys files if they follow a pattern, though .env covers it.

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Large/Binary files from evaluations (optional, but good practice)
*.pdf
*.docx
